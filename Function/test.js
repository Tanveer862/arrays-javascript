const vowelCount = (str) => {
    let count = 0;
    for (const char of str) {
        if (char === "a" || char === "e" || char === "i" || char === "o" || char === "u") {
            count++
        }
    }
    return count;
};

const vowelCounter = (str) => {
    for (const char of str) {
        if (char === "a" || char === "e" || char === "i" || char === "o" || char === "u") {
            count++
        }
    }
    return count;
};

let test = vowelCount("Tanveerhussain")
console.log(test)
function abc() {
    var a = 2;
    
    var a=0
    
    {

        console.log(a)
    }
    console.log(a)
}
abc()







////
function information(){
    let name="Tanveer"
    let fName="Hussain"
    let email="tanveerhussain7767235@gmail.com"
    console.log(name)
    console.log(fName)
    console.log(email)
}
information()
